<!DOCTYPE html>
<html lang="en">
<head>
    <title>Journals</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" type="image/png" href="/images/dart2.ico" />
    <link rel="stylesheet" href="/css/main2.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
          integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet">
    <style>
        .red{
            background-color: #21bcc7;
        }
    </style>
</head>
<body>
    <section id="cart-section">
        <div class="container">
            <div class="jumbotron red">
                <h2 id="title" style="margin-top: 50px;">Register</h2>
                <div id="container2"> 
                    <% if(errors != "") { %>
                        <% errors.forEach((item) => { %>
                            <div class="alert alert-dark" style="font-size: 10px !important; margin-top: 0px;">
                                <%- item %>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
                    
                <form method="POST" action="/register">
                        
                        <div class="form-group" style="margin-bottom: 10px; margin-top: 20px;" >
                        <label for="fullName" >Full name:</label>
                        <input type="text" class="form-control" name="fullName" id="fullName" placeholder="Full name" value="<%=newUser.full_name%>" required>
                        </div>
                        <div class="form-group" style="margin-bottom: 10px;">
                            <label for="display_name" style="margin-bottom: -20px;">Display name:</label><br>
                            <small style="color:white; font-size:10px;">Spaces are not allowed.</small>
                            <input type="text" name="display_name" class="form-control" id="display_name" placeholder="Enter Display Name" pattern="[^' ']+" value="<%=newUser.display_name%>" required>
                        </div>
                        <div class="form-group" style="margin-bottom: -10px;">
                            <label for="birthday" style="margin-bottom: -20px;">Birthday</label><br>
                            <small style="color:white; font-size:10px;">At least 13 years old.</small>
                            <br>
                            <input type="date" id="birthday" name="birthday" value="<%=newUser.birthday%>" required>
                        </div>
                        <div class="form-group" style="margin-bottom: 20px; margin-top: 20px;">
                            <label for="email_address">Email address:</label>
                            <input type="text" name="email_address" class="form-control" id="email_address" placeholder="Enter Email" pattern="[^' ']+" value="<%=newUser.email_address%>" required>
                            </div>
                        <div class="form-group" >
                            <label for="password" style="margin-top: -20px;">Password:</label>
                            <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                            <input type="checkbox" onclick="myFunction()"> <p style="color: white; margin-top: -21px; margin-left: 20px;">Show Password</p>
                        </div>

                        <div class="form-group" >
                            <label for="passwordConfirmation" style="font-size: 14px; margin-top: -10px;">Confirm Password:</label>
                            <input type="password" class="form-control" id="passwordConfirmation" name="passwordConfirmation" placeholder="Password" required>
                            <input type="checkbox" onclick="myFunction2()"> <p style="color: white; margin-top: -21px; margin-left: 20px;">Show Password</p>
                        </div>

                        <div class="button-area column" style="margin-top: -10px;"><button id="registerbutton" type="submit" >Register</button>
                    </form>
                
                    <div class="account-area">
                    <p>Have an account already? <a href="/login">Login Here</a></p>
                    </div>
                </div>
  
            </div>       
        </div>
    </section>

<script>
    
function myFunction() {
  var x = document.getElementById("password");
  if (x.type === "password") {
    x.type = "text";
  } else {
    x.type = "password";
  }
}

function myFunction2() {
  var x = document.getElementById("passwordConfirmation");
  if (x.type === "password") {
    x.type = "text";
  } else {
    x.type = "password";
  }
}

$(function(){
        var dtToday = new Date();
    
        var month = dtToday.getMonth() + 1;// jan=0; feb=1 .......
        var day = dtToday.getDate();
        var year = dtToday.getFullYear() - 13;
        if(month < 10)
            month = '0' + month.toString();
        if(day < 10)
            day = '0' + day.toString();
    	var minDate = year + '-' + month + '-' + day;
        var maxDate = year + '-' + month + '-' + day;
    	$('#birthday').attr('max', maxDate);
    });


    const usernameInput = document.querySelector('input[type="display_name"]');
    usernameInput.addEventListener("input", function(e){
    e.target.value = e.target.value.toLowerCase();
    });
</script>
</body>
</html>
